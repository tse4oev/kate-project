# 🚀 Руководство по деплою Katya Pocket на Netlify

## ✅ Подготовка завершена

Создан готовый к деплою архив: `katya-pocket-netlify-deploy.zip`

### 📦 Содержимое архива:
- `index.html` - главная страница (базовый путь исправлен: `<base href="/">`)
- `main-UG5QRENH.js` - скомпилированное приложение
- `index.css` - стили
- `favicon.ico` - иконка
- `manifest.webmanifest` - PWA манифест
- `.nojekyll` - для корректной работы на Netlify
- `404.html` - для SPA роутинга
- `prerendered-routes.json` - предрендеренные маршруты
- `3rdpartylicenses.txt` - лицензии

## 🌐 Инструкция по деплою на Netlify

### Способ 1: Drag & Drop (простейший)
1. Перейдите на [netlify.com](https://netlify.com)
2. Войдите в аккаунт или создайте новый
3. На главной странице найдите зону "Deploy to Netlify"
4. Перетащите файл `katya-pocket-netlify-deploy.zip` в эту область
5. Дождитесь завершения загрузки и деплоя

### Способ 2: Через панель управления
1. Войдите в панель Netlify
2. Нажмите "Add new site" → "Deploy manually"
3. Выберите файл `katya-pocket-netlify-deploy.zip`
4. Дождитесь завершения деплоя

### Способ 3: Git интеграция (продвинутый)
1. Загрузите содержимое каталога `netlify-deploy/` в GitHub репозиторий
2. В Netlify выберите "Add new site" → "Import from Git"
3. Подключите GitHub и выберите репозиторий
4. Настройки сборки:
   - Build command: оставить пустым
   - Publish directory: оставить пустым (или указать корень)
5. Деплой будет происходить автоматически при каждом push

## ⚙️ Настройки Netlify

### Базовые настройки (обычно не нужны):
- **Build command**: (оставить пустым)
- **Publish directory**: (оставить пустым или указать корень)
- **Node version**: любая (так как уже собрано)

### Дополнительные настройки для SPA:
Если приложение использует маршрутизацию, убедитесь, что включен redirect:
- Файл `404.html` уже настроен для корректной работы SPA

### Пользовательский домен (опционально):
1. В настройках сайта выберите "Domain management"
2. Добавьте свой домен или используйте предоставленный Netlify

## 🎯 После деплоя

1. **Проверьте работоспособность:**
   - Откройте сайт в браузере
   - Проверьте загрузку всех ресурсов
   - Протестируйте навигацию

2. **Мониторинг:**
   - В панели Netlify можно смотреть логи
   - Отслеживайте метрики производительности

3. **Обновления:**
   - Для новых версий просто загружайте новый архив
   - Или настройте Git интеграцию для автоматических деплоев

## 🔧 Решение проблем

### Если сайт не загружается:
1. Проверьте логи в панели Netlify
2. Убедитесь, что базовый путь установлен корректно (`<base href="/">`)
3. Проверьте консоль браузера на ошибки

### Если маршрутизация не работает:
1. Убедитесь, что файл `404.html` присутствует
2. Проверьте настройки redirect в Netlify

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте логи в панели Netlify
2. Убедитесь, что все файлы загружены корректно
3. Проверьте настройки домена и SSL

---

**✅ Архив готов к деплою! Просто перетащите `katya-pocket-netlify-deploy.zip` на Netlify.**
