# Katya Pocket 🌸

**Ваше уютное персональное пространство для заметок, мини-игр, советов и музыки. Создано, чтобы дарить спокойствие и вдохновение.**

"Katya Pocket" — это полностью автономное веб-приложение (PWA), которое работает офлайн и не зависит от внешних серверов. Все ваши данные хранятся локально в браузере, обеспечивая полную приватность.

## ✨ Особенности

- **Полная автономность**: Работает без интернета. Все функции, кроме встраивания Spotify, доступны офлайн.
- **Приватно и безопасно**: Все данные (заметки, прогресс игр, настройки) хранятся только на вашем устройстве в LocalStorage.
- **Прогрессивное веб-приложение (PWA)**: Можно установить на рабочий стол или главный экран смартфона для быстрого доступа.
- **Минималистичный тёмный интерфейс**: Уютный дизайн, который помогает сосредоточиться и расслабиться.
- **Локальный "AI"-помощник**: Раздел "Советы" работает на основе локального набора правил и не требует подключения к сети.
- **Готовность к расширению**: Простая и понятная архитектура на Angular Signals.

## 🛠️ Архитектура и стек

- **Фреймворк**: Angular 20+ (Standalone Components, Signals, Zoneless)
- **Стилизация**: Tailwind CSS
- **Хранение данных**: LocalStorage (через универсальный `DataService`)
- **PWA**: Angular Service Worker для кэширования и офлайн-работы.

Проект построен на модульной архитектуре с разделением на `core` (сервисы) и `features` (компоненты).

## 🚀 Установка и запуск

Для запуска проекта локально вам потребуется Node.js и npm.

1.  **Клонируйте репозиторий (или скачайте архив):**
    ```bash
    git clone https://github.com/your-username/katya-pocket.git
    cd katya-pocket
    ```

2.  **Установите зависимости:**
    ```bash
    npm install
    ```

3.  **Запустите приложение в режиме разработки:**
    ```bash
    npm run start 
    ```
    После этого откройте в браузере `http://localhost:4200/`. Приложение будет автоматически перезагружаться при изменении файлов.

## 🌐 Деплой на GitHub Pages

Проект полностью готов к публикации на бесплатном хостинге GitHub Pages.

Приложение уже опубликовано и доступно по адресу: **https://tse4oev.github.io/kate-project/**

Для повторного деплоя:

1.  **Соберите приложение для продакшена.** Замените `your-repo-name` на имя вашего репозитория.
    ```bash
    npm run build -- --configuration=production --base-href=/your-repo-name/
    ```
    Эта команда создаст папку `build` с готовыми файлами.

2.  **Установите `angular-cli-ghpages` для деплоя:**
    ```bash
    npm install -g angular-cli-ghpages
    ```

3.  **Опубликуйте приложение:**
    ```bash
    npx angular-cli-ghpages --dir=build --repo=https://github.com/tse4oev/kate-project.git
    ```

Через несколько минут ваше приложение будет доступно по адресу `https://tse4oev.github.io/kate-project/`.

## ✅ Тестирование офлайн-режима

1.  Запустите приложение (`npm run start` или откройте версию на GitHub Pages).
2.  Откройте инструменты разработчика в браузере (F12).
3.  Перейдите на вкладку `Network` (Сеть).
4.  Включите опцию `Offline`.
5.  Обновите страницу. Приложение должно загрузиться и работать, используя кэш сервис-воркера.
